{
  "name": "burn0",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node scripts/check.mjs",
    "db:migrate:local": "wrangler d1 migrations apply burn0-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply burn0-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
